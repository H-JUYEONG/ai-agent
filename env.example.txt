# OpenAI API Key (필수)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Tavily API Key (필수)
TAVILY_API_KEY=tvly-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Serper API Key (필수 - 검색 백업)
SERPER_API_KEY=your_serper_api_key_here

# LangSmith API Key (선택 - 디버깅용)
LANGSMITH_API_KEY=lsv2_pt_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGSMITH_TRACING=true
LANGSMITH_PROJECT=ai-service-advisor

# 애플리케이션 설정
DEBUG=false

# ========================================
# Redis & Qdrant 설정
# ========================================
# ⚠️ 주의: Docker Compose 사용 시 (EC2 배포 등) 이 설정은 무시됩니다!
# Docker Compose가 자동으로 설정하므로 입력 불필요
#
# 📝 로컬 개발 시 (Python 직접 실행)만 필요:
#    1. Redis 실행: docker run -d -p 6379:6379 redis
#    2. Qdrant 실행: docker run -d -p 6333:6333 qdrant/qdrant
#    3. Python 실행: uvicorn app.main:app --reload
# ========================================

# Redis 설정 (로컬 개발 시에만 사용)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Qdrant 설정 (로컬 개발 시에만 사용)
QDRANT_URL=localhost
QDRANT_PORT=6333
# QDRANT_API_KEY=your_qdrant_api_key_here  # 클라우드 사용 시에만



