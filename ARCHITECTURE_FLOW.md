# 🏗️ AI Agent 아키텍처 및 워크플로우

## 📋 목차
1. [새로 추가된 기능](#새로-추가된-기능)
2. [전체 워크플로우](#전체-워크플로우)
3. [각 단계별 상세 설명](#각-단계별-상세-설명)
4. [데이터 흐름](#데이터-흐름)

---

## 🆕 새로 추가된 기능

### 1. **Fact Model** (`app/agent/models.py`)
**목적**: 구조화된 도구 정보를 저장하는 데이터 모델

**주요 클래스:**
- `ToolFact`: 도구의 모든 사실 정보 (가격, 통합, 언어, 보안 등)
- `PricingPlan`: 가격 플랜 정보
- `UserContext`: 사용자 맥락 (팀 규모, 기술 스택, 예산 등)
- `ToolScore`: 도구별 점수
- `DecisionResult`: 최종 판단 결과

**특징:**
- Pydantic 모델로 타입 안정성 보장
- Enum 타입으로 표준화된 값 관리 (`SecurityPolicy`, `WorkflowType`)

### 2. **Fact Extractor** (`app/agent/fact_extractor.py`)
**목적**: 웹 검색 결과(Findings)에서 구조화된 사실을 추출

**동작 방식:**
1. LLM에게 Findings를 전달
2. JSON 형식으로 구조화된 정보 추출 요청
3. `ToolFact` 객체 리스트로 변환

**추출하는 정보:**
- 도구명
- 가격 플랜 (플랜명, 가격, 타입)
- 통합 기능 (GitHub, GitLab 등)
- 지원 언어
- 보안 정책
- 업무 지원 타입 (코드 작성, 리뷰 등)
- 출처 URL

### 3. **Decision Engine** (`app/agent/decision.py`)
**목적**: 사용자 맥락을 기반으로 도구를 필터링하고 점수를 계산하여 추천

**주요 메서드:**
- `filter_tools()`: 하드 제약 조건으로 필터링
  - 제외 목록 확인
  - 보안 요구사항 확인
  - 언어 지원 확인
  - 통합 기능 확인
  - 예산 확인

- `calculate_score()`: 도구별 점수 계산
  - 언어 지원 점수 (30%)
  - 통합 기능 점수 (20%)
  - 업무 적합성 점수 (20%)
  - 가격 점수 (15%)
  - 보안 점수 (15%)

- `remove_duplicate_features()`: 중복 기능 도구 제거
  - 같은 기능 카테고리(code_completion, code_review 등)에서 점수가 높은 것만 선택

- `make_decision()`: 최종 판단
  - 필터링 → 점수 계산 → 중복 제거 → 정렬 → 추천

### 4. **State 확장** (`app/agent/state.py`)
**새로 추가된 필드:**
- `tool_facts`: 구조화된 도구 사실 리스트
- `decision_result`: Decision Engine의 판단 결과

---

## 🔄 전체 워크플로우

```
사용자 메시지 입력
    ↓
┌─────────────────────────────────────┐
│ 1. clarify_with_user               │
│    - 주제 관련성 확인                │
│    - 명확화 필요 여부 판단           │
│    - 캐시 확인                      │
└─────────────────────────────────────┘
    ↓ (명확화 불필요)
┌─────────────────────────────────────┐
│ 2. write_research_brief             │
│    - 연구 질문 생성                 │
│    - 하드 제약 조건 추출             │
│    - 질문 유형 분류                 │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 3. research_supervisor              │
│    - 연구 계획 수립                  │
│    - 병렬 연구 위임                 │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 4. researcher (병렬 실행)           │
│    - 웹 검색 수행                    │
│    - 정보 수집                       │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 5. compress_research                │
│    - 연구 결과 압축                  │
│    - Findings 생성                   │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 6. final_report_generation          │
│    ├─ 🆕 extract_tool_facts()       │
│    │   └─ Findings → ToolFact[]     │
│    │                                 │
│    ├─ 🆕 DecisionEngine              │
│    │   ├─ UserContext 생성           │
│    │   ├─ filter_tools()             │
│    │   ├─ calculate_score()          │
│    │   ├─ remove_duplicate_features()│
│    │   └─ make_decision()            │
│    │                                 │
│    └─ 최종 리포트 생성                │
│       (Decision Engine 결과 반영)    │
└─────────────────────────────────────┘
    ↓
최종 답변 반환
```

---

## 📝 각 단계별 상세 설명

### 1. **clarify_with_user**
- **입력**: 사용자 메시지
- **처리**:
  - 주제 관련성 확인 (코딩 AI 도구 추천 관련인가?)
  - Follow-up 여부 확인
  - 명확화 필요 여부 판단 (팀 규모, 예산, 언어 등)
  - 캐시 확인 (이전에 같은 질문이 있었는지)
- **출력**: 명확화 질문 또는 연구 시작 확인 메시지

### 2. **write_research_brief**
- **입력**: 사용자 메시지, 이전 대화
- **처리**:
  - 연구 질문 생성 (Research Brief)
  - 하드 제약 조건 추출:
    - 예산 제약
    - 보안 요구사항
    - 제외할 도구
    - 팀 규모
    - 필수 지원 항목
  - 질문 유형 분류:
    - `decision`: 의사결정 요청
    - `comparison`: 비교 요청
    - `explanation`: 설명 요청
    - `information`: 정보 요청
    - `guide`: 가이드 요청
- **출력**: `ResearchQuestion` (research_brief, question_type, hard_constraints)

### 3. **research_supervisor**
- **입력**: Research Brief
- **처리**:
  - 연구 계획 수립
  - 필요한 정보 파악 (가격, 통합, 보안 등)
  - 병렬 연구 위임 (최대 3개)
- **출력**: 연구 지시사항

### 4. **researcher** (병렬 실행)
- **입력**: 연구 주제
- **처리**:
  - 웹 검색 수행 (Tavily, Serper.dev)
  - 공식 사이트 우선 검색
  - 정보 수집 (가격, 기능, 통합 등)
- **출력**: 연구 노트 (raw_notes)

### 5. **compress_research**
- **입력**: 연구 노트들
- **처리**:
  - 연구 결과 압축
  - Findings 생성 (텍스트 형식)
- **출력**: 압축된 Findings

### 6. **final_report_generation** ⭐ **핵심 단계**
- **입력**: Findings, 사용자 제약 조건, 질문 유형
- **처리**:

  #### 6-1. **Fact Extraction** 🆕
  ```python
  tool_facts = await extract_tool_facts(findings, config)
  # Findings (텍스트) → ToolFact[] (구조화된 데이터)
  ```
  - LLM이 Findings에서 구조화된 정보 추출
  - 각 도구별로 `ToolFact` 객체 생성
  - State에 저장: `state["tool_facts"] = tool_facts`

  #### 6-2. **Decision Engine** 🆕 (질문 유형이 "decision"인 경우)
  ```python
  # UserContext 생성
  user_context = UserContext(
      team_size=8,
      tech_stack=["Java", "JavaScript"],
      budget_max=100000,
      security_required=False,
      workflow_focus=[WorkflowType.CODE_GENERATION],
      excluded_tools=[]
  )
  
  # Decision Engine 실행
  engine = DecisionEngine(user_context)
  decision_result = engine.make_decision(tool_facts)
  ```
  
  **Decision Engine 내부 동작:**
  1. **필터링** (`filter_tools`)
     - 제외 목록 확인
     - 보안 요구사항 확인
     - 언어 지원 확인
     - 통합 기능 확인
     - 예산 확인
   
  2. **점수 계산** (`calculate_score`)
     - 각 도구별로 5가지 항목 점수 계산
     - 가중 평균으로 총점 계산
   
  3. **중복 제거** (`remove_duplicate_features`)
     - 같은 기능 카테고리에서 점수가 높은 것만 선택
     - 예: code_completion 도구가 여러 개면 점수가 가장 높은 것만 선택
   
  4. **정렬 및 추천**
     - 점수 순으로 정렬
     - 상위 도구 추천

  #### 6-3. **최종 리포트 생성**
  - Decision Engine 결과를 프롬프트에 포함
  - LLM이 최종 리포트 생성
  - 추천 도구, 제외된 도구, 판단 이유 포함

- **출력**: 최종 리포트 (마크다운 형식)

---

## 📊 데이터 흐름

### State 변화 과정

```python
# 1. 초기 State
AgentState(
    messages=[...],
    domain="코딩",
    constraints=None,
    question_type=None,
    tool_facts=[],  # 빈 리스트
    decision_result=None
)

# 2. write_research_brief 후
AgentState(
    ...
    constraints={
        "team_size": 8,
        "budget_max": 100000,
        "must_support_language": ["Java", "JavaScript"]
    },
    question_type="decision"
)

# 3. final_report_generation - Fact Extraction 후
AgentState(
    ...
    tool_facts=[
        {
            "name": "GitHub Copilot",
            "pricing_plans": [...],
            "integrations": ["GitHub", "Slack"],
            "supported_languages": ["Python", "JavaScript", "Java"],
            ...
        },
        {
            "name": "Tabnine",
            ...
        }
    ]
)

# 4. final_report_generation - Decision Engine 후
AgentState(
    ...
    decision_result={
        "recommended_tools": ["GitHub Copilot", "Cursor"],
        "excluded_tools": ["Tabnine"],
        "tool_scores": [
            {
                "tool_name": "GitHub Copilot",
                "total_score": 0.85,
                "language_support_score": 1.0,
                ...
            },
            ...
        ],
        "reasoning": {
            "GitHub Copilot": "기술 스택 완벽 지원, 예산 범위 내",
            "Tabnine": "기능 중복으로 제외"
        }
    }
)
```

---

## 🎯 핵심 개선 사항

### Before (프롬프트만 사용)
- ❌ 정보 수집 → 요약만 하고 끝
- ❌ 판단 기준이나 점수 계산 없음
- ❌ 사용자 맥락에 따른 논리적 decision flow 없음
- ❌ "둘 다 괜찮아요" 식의 중립적 답변

### After (Fact Model + Decision Engine)
- ✅ 정보 수집 → 구조화된 사실 저장
- ✅ 명확한 판단 기준과 점수 계산
- ✅ 사용자 맥락 기반 논리적 판단
- ✅ 명확한 추천과 제외 이유 제공
- ✅ 중복 기능 도구 자동 제거

---

## 🔍 예시 시나리오

### 사용자 질문
> "8명 규모의 웹 개발팀(Spring Boot + React)에 적합한 AI 도구 추천해줘"

### 처리 과정

1. **clarify_with_user**: 주제 확인, 명확화 불필요
2. **write_research_brief**: 
   - Research Brief 생성
   - `constraints = {team_size: 8, must_support_language: ["Java", "JavaScript"]}`
   - `question_type = "decision"`
3. **research_supervisor**: 연구 계획 수립
4. **researcher**: 웹 검색으로 도구 정보 수집
5. **compress_research**: Findings 생성
6. **final_report_generation**:
   - **Fact Extraction**: Findings → ToolFact[]
     ```python
     [
       ToolFact(name="GitHub Copilot", pricing_plans=[...], ...),
       ToolFact(name="Tabnine", ...),
       ToolFact(name="Cursor", ...)
     ]
     ```
   - **Decision Engine**:
     - UserContext: `{team_size: 8, tech_stack: ["Java", "JavaScript"], ...}`
     - 필터링: 모든 도구 통과
     - 점수 계산:
       - GitHub Copilot: 0.85점 (Java, JavaScript 지원, GitHub 통합)
       - Cursor: 0.80점
       - Tabnine: 0.75점
     - 중복 제거: Tabnine 제외 (Copilot과 같은 code_completion 기능)
     - 추천: ["GitHub Copilot", "Cursor"]
   - **최종 리포트 생성**: Decision Engine 결과 반영하여 리포트 생성

### 최종 답변
> "8명 규모의 Spring Boot + React 팀에는 **GitHub Copilot**을 추천합니다.
> 
> **추천 이유:**
> - Java, JavaScript 완벽 지원
> - GitHub 통합으로 팀 협업 효율적
> - 예산 범위 내 ($19/월/인)
> 
> **제외된 도구:**
> - Tabnine: Copilot과 기능 중복"

---

## 📁 파일 구조

```
app/agent/
├── models.py              # 🆕 Fact Model 정의
├── fact_extractor.py      # 🆕 Findings → ToolFact 추출
├── decision.py            # 🆕 Decision Engine
├── state.py              # ✏️ tool_facts, decision_result 추가
├── nodes.py              # ✏️ final_report_generation 수정
├── prompts.py            # (기존)
├── graph.py              # (기존)
└── ...
```

---

## 🚀 다음 단계 (TODO)

1. **사용자 메시지에서 더 정확한 정보 추출**
   - `required_integrations` 추출
   - `workflow_focus` 더 정확히 추출

2. **LangGraph 기반 사실 저장**
   - 도구 사실을 그래프로 저장
   - 관계 추적 (통합, 언어 지원 등)

3. **신뢰 점수 계산**
   - 출처 신뢰도 기반 점수 조정


