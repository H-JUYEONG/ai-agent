# 변경 사항 (Changelog)

이 문서는 기존 프로젝트에서 발견된 버그 수정 및 부족했던 기능 보강 사항을 정리합니다.

---

## 📋 목차

1. [버그 수정](#버그-수정)
2. [기능 보강](#기능-보강)
3. [라이브러리 업데이트](#라이브러리-업데이트)
4. [성능 개선](#성능-개선)

---

## 🐛 버그 수정

### 1. 캐시 히트율 낮음 문제

**문제점**:
- 의미적으로 동일한 질문이 다른 캐시 키로 저장되어 캐시 히트율이 낮았음
- 예: "Python 코딩 AI 추천해줘"와 "Python 개발용 AI 도구 추천해줘"가 다른 캐시 키로 저장됨

**해결 방법**:
- **Query Normalizer 모듈 추가** (`app/tools/query_normalizer.py`)
- LLM 기반 쿼리 정규화로 의미적으로 동일한 질문을 동일한 형식으로 변환
- 정규화된 텍스트와 키워드 기반 캐시 키 생성

**결과**:
- 캐시 히트율 대폭 향상
- 유사 질문 응답 시간: ~10초 → ~0.1초 (99% 개선)

---

### 2. 캐시된 답변 검증 부족

**문제점**:
- 캐시된 답변이 비어있거나 너무 짧아도 그대로 사용
- 리포트 본문이 유효하지 않은 경우에도 캐시 사용

**해결 방법**:
- 캐시된 답변 검증 로직 추가 (`app/agent/nodes.py`)
- 리포트 본문 최소 길이 검증 (200자 이상)
- 유효하지 않은 캐시는 무시하고 새로 생성

**코드 위치**: `app/agent/nodes.py` - `clarify_with_user` 함수

---

### 3. 첫 질문에 인사말 없음

**문제점**:
- 첫 질문에 인사말 없이 바로 답변이 나옴
- 두 번째 질문부터는 인사말이 나오지만 일관성 없음

**해결 방법**:
- 인사말/리포트 분리 로직 개선 (`app/agent/nodes.py`, `app/routes/chat.py`)
- `[GREETING]` 태그 지원 추가
- 캐시에 인사말이 없으면 키워드 기반으로 자동 생성

**코드 위치**: 
- `app/agent/nodes.py` - `final_report_generation`, `clarify_with_user` 함수
- `app/routes/chat.py` - 메시지 처리 로직

---

### 4. Follow-up 질문 처리 부족

**문제점**:
- Follow-up 질문에서 이전 추천 도구를 고려하지 않음
- 캐시된 답변의 도구가 이전 추천 도구와 다를 때도 캐시 사용

**해결 방법**:
- 이전 추천 도구 추출 로직 추가 (다양한 패턴 지원)
- 캐시된 답변의 도구와 이전 추천 도구 비교
- 도구가 다르면 캐시 무시하고 새로 생성

**코드 위치**: `app/agent/nodes.py` - `clarify_with_user` 함수

---

### 5. 중복 답변 문제

**문제점**:
- 동일한 내용의 메시지가 중복으로 반환됨
- 인사말과 리포트가 제대로 분리되지 않음

**해결 방법**:
- 메시지 중복 제거 로직 개선 (`app/routes/chat.py`)
- 메시지 길이 기반 분리 로직 개선
- AI 메시지 추출 및 순서 처리 개선

**코드 위치**: `app/routes/chat.py` - `chat` 함수

---

### 6. 웹 검색 반복 문제

**문제점**:
- 동일한 정보를 매번 웹 검색하여 비용 및 시간 낭비
- 검색 결과를 재사용하지 않음

**해결 방법**:
- **Vector DB (Qdrant) 통합** (`app/tools/vector_store.py`)
- 웹 검색 결과를 Facts로 변환하여 Vector DB에 저장 (TTL 30일)
- 유사한 질문에 대해 Vector DB에서 먼저 검색

**결과**:
- 웹 검색 횟수 대폭 감소
- 관련 질문 응답 시간: ~10초 → ~2초 (80% 개선)

---

### 7. 캐시 키 해시 중복 문제

**문제점**:
- `research_cache.get()` 및 `set()`에서 이미 해시된 캐시 키를 다시 해시함
- 캐시 키가 올바르게 매칭되지 않음

**해결 방법**:
- `_get_key()` 메서드에서 이미 해시 형태인지 확인
- 해시 형태면 그대로 사용, 아니면 해시 생성

**코드 위치**: `app/tools/cache.py` - `_get_key` 메서드

---

## 🔧 기능 보강

### 1. 하이브리드 캐싱 시스템 구축

**기존 부족점**:
- 검색 결과만 캐싱 (24시간 TTL)
- 최종 답변은 매번 새로 생성
- 의미적으로 동일한 질문이 다른 캐시로 저장

**보강 내용**:
- **3단계 캐싱 시스템** 구축:
  1. **Query Normalizer**: 질문 정규화 및 캐시 키 생성
  2. **Redis 최종 답변 캐싱**: 7일 TTL
  3. **Qdrant Vector DB Facts 저장**: 30일 TTL

**파일**:
- `app/tools/query_normalizer.py` (신규)
- `app/tools/cache.py` (수정)
- `app/tools/vector_store.py` (신규)

---

### 2. 지능형 검색 전략 구현

**기존 부족점**:
- 항상 웹 검색부터 시작
- 검색 결과 재사용 불가

**보강 내용**:
- **Vector DB 우선 검색** 전략 구현
- Vector DB에서 충분한 정보가 있으면 웹 검색 생략
- 웹 검색 결과를 Vector DB에 저장하여 재사용

**파일**: `app/agent/nodes.py` - `researcher`, `researcher_tools` 함수

---

### 3. 응답 포맷팅 개선

**기존 부족점**:
- 단일 메시지로 응답
- 인사말과 리포트 본문 구분 없음
- 가독성 낮음

**보강 내용**:
- **2개 메시지 버블**로 분리 (인사말 + 리포트)
- `[GREETING]` 태그 지원
- 키워드 기반 동적 인사말 생성

**파일**:
- `app/agent/prompts.py` - 리포트 생성 프롬프트 수정
- `app/agent/nodes.py` - 인사말/리포트 분리 로직
- `app/routes/chat.py` - 메시지 처리 로직

---

### 4. Follow-up 질문 컨텍스트 관리

**기존 부족점**:
- Follow-up 질문을 처음 질문과 동일하게 처리
- 이전 추천 도구 정보 활용 안 함

**보강 내용**:
- 이전 추천 도구 추출 로직 (다양한 패턴 지원)
- Follow-up 질문에서 이전 도구를 컨텍스트로 활용
- 캐시 검증 시 이전 추천 도구와 비교

**파일**: `app/agent/nodes.py` - `clarify_with_user`, `write_research_brief`, `final_report_generation` 함수

---

### 5. 데이터 저장 확인 도구 추가

**기존 부족점**:
- Redis 및 Vector DB 저장 데이터 확인 방법 없음
- 디버깅 어려움

**보강 내용**:
- **`check_storage.py` 스크립트 추가**
- Redis 및 Vector DB 통계 조회
- 샘플 데이터 확인

**파일**: `check_storage.py` (신규)

---

### 6. 환경 변수 관리 개선

**기존 부족점**:
- Qdrant 설정 방법 불명확
- Docker Compose와 로컬 실행 환경 변수 혼동

**보강 내용**:
- `env.example.txt`에 Qdrant 설정 추가
- Docker Compose 사용 시 자동 설정 안내
- 로컬 개발 시 수동 설정 가이드 추가

**파일**: `env.example.txt` (수정)

---

## 📦 라이브러리 업데이트

### LangChain & LangGraph 1.0 이상 적용

**기존 버전**:
- LangChain 0.x 버전 사용
- LangGraph 0.x 버전 사용

**업데이트 내용**:
- **LangChain 1.0 이상** 적용 (`langchain>=1.0.0,<2.0.0`)
- **LangGraph 1.0 이상** 적용 (`langgraph>=1.0.0,<2.0.0`)
- **LangChain OpenAI 1.0 이상** 적용 (`langchain-openai>=1.0.0,<2.0.0`)

**변경 사항**:
- API 변경사항 반영
- 최신 기능 활용

**파일**: `requirements.txt`

---

### Python 버전 명시

**기존**:
- Python 버전 명시 없음

**업데이트 내용**:
- **Python 3.11 이상** 권장 (Dockerfile)
- **Python 3.12** 지원 (로컬 개발)

**파일**:
- `Dockerfile` - Python 3.11-slim
- `README.md` - Python 3.12 가이드

---

### 추가된 의존성

**신규 추가**:
- `qdrant-client==1.7.0` - Vector DB 클라이언트
- `sentence-transformers>=2.7.0,<3.0.0` - 임베딩 모델

**파일**: `requirements.txt`

---

## ⚡ 성능 개선

### 1. 응답 시간 개선

| 시나리오 | 기존 | 개선 후 | 개선율 |
|---------|------|---------|--------|
| 첫 질문 | ~10초 | ~10초 | - |
| 유사 질문 (같은 의미) | ~10초 | ~0.1초 | **99% 개선** |
| 관련 질문 (다른 표현) | ~10초 | ~2초 | **80% 개선** |

**개선 방법**:
- Query Normalizer로 캐시 히트율 향상
- Vector DB로 웹 검색 최소화

---

### 2. 웹 검색 횟수 감소

**기존**:
- 모든 질문마다 웹 검색 수행

**개선 후**:
- Vector DB에서 충분한 정보가 있으면 웹 검색 생략
- 웹 검색 횟수 **대폭 감소**

---

### 3. 캐시 히트율 향상

**기존**:
- 의미적으로 동일한 질문이 다른 캐시로 저장
- 캐시 히트율 낮음

**개선 후**:
- Query Normalizer로 의미적으로 동일한 질문 통합
- 캐시 히트율 **대폭 향상**

---

## 📝 추가된 파일

### 1. `app/tools/vector_store.py`
- Qdrant Vector DB 클라이언트
- Facts 저장/검색 관리

### 2. `app/tools/query_normalizer.py`
- LLM 기반 쿼리 정규화
- 캐시 키 생성

### 3. `check_storage.py`
- Redis 및 Vector DB 데이터 확인 스크립트

### 4. `VECTOR_DB_GUIDE.md`
- Vector DB 통합 가이드

### 5. `CHANGELOG.md`
- 변경 사항 기록 (이 문서)

---

## 🔄 수정된 파일

### 1. `app/tools/cache.py`
- `prefix` 파라미터 추가
- TTL 자동 선택 로직
- 최종 답변 캐싱 지원

### 2. `app/tools/__init__.py`
- `vector_store`, `query_normalizer` export 추가

### 3. `app/agent/nodes.py`
- Query Normalizer 통합
- Vector DB 검색/저장
- 최종 답변 캐싱
- 인사말/리포트 분리
- Follow-up 질문 처리 개선

### 4. `app/agent/prompts.py`
- `[GREETING]` 태그 지원
- 리포트 포맷팅 개선
- Query Normalizer 프롬프트 추가

### 5. `app/routes/chat.py`
- 메시지 처리 로직 개선
- 인사말/리포트 분리

### 6. `requirements.txt`
- LangChain/LangGraph 1.0 이상 적용
- Qdrant, Sentence Transformers 추가

### 7. `docker-compose.yml`
- Qdrant 서비스 추가

### 8. `env.example.txt`
- Qdrant 설정 추가

### 9. `README.md`
- Vector DB 통합 설명
- 성능 개선 수치 추가

---

## 🗑️ 삭제된 파일

### 1. `app/routes/storage.py`
- **삭제 이유**: 웹 UI로 저장 데이터 확인 불필요
- **대체 방법**: `check_storage.py` 스크립트 사용

### 2. `app/templates/storage.html`
- **삭제 이유**: `storage.py`와 함께 삭제

---

## 🎯 주요 개선 사항 요약

1. **캐시 시스템 개선**: Query Normalizer + Redis + Vector DB 하이브리드 캐싱
2. **검색 전략 개선**: Vector DB 우선 검색으로 웹 검색 최소화
3. **응답 품질 개선**: 인사말/리포트 분리, Follow-up 질문 컨텍스트 관리
4. **라이브러리 업데이트**: LangChain/LangGraph 1.0 이상 적용
5. **버그 수정**: 캐시 검증, 중복 답변, Follow-up 처리 등

---

**작성일**: 2025-01-XX  
**버전**: 2.0.0
